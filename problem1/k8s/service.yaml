apiVersion: v1
kind: Service
metadata:
  name: wisecow-service
  namespace: wisecow
  labels:
    app: wisecow
spec:
  selector:
    app: wisecow
  ports:
    - name: http
      protocol: TCP
      port: 4499
      targetPort: 4499
  type: ClusterIP
